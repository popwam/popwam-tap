{
  "name": "@popwam/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "prisma generate && tsc --noEmit",
    "lint": "prisma validate && tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "6.19.1",
    "bcryptjs": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "dotenv": "^17.2.2",
    "prisma": "6.19.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
