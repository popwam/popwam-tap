# POPWAM Tap bundled branding fallbacks

Files in this directory are production-safe static fallbacks. Dynamic branding uploaded from `/admin/branding` is stored in Cloudflare R2 and takes precedence.
