import {prisma} from "@popwam/db";
import {csrfRejected,getApiUser,isSameOriginMutation,unauthorized} from "@/lib/api-auth";
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){if(!isSameOriginMutation(request))return csrfRejected();const user=await getApiUser();if(!user)return unauthorized();const {id}=await params;const body=await request.json().catch(()=>({}));const account=await prisma.connectedAccount.findFirst({where:{id,userId:user.id}});if(!account)return Response.json({error:"NOT_FOUND"},{status:404});await prisma.$transaction(async tx=>{if(body.removeImportedData===true)await tx.cardImportedField.deleteMany({where:{connectedAccountId:id}});await tx.connectedAccount.update({where:{id},data:{status:"REVOKED",accessTokenEncrypted:null,refreshTokenEncrypted:null,disconnectedAt:new Date()}})});return Response.json({ok:true,importedDataKept:body.removeImportedData!==true})}
