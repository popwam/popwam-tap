import {prisma} from "@popwam/db";import {getMobileUser,mobileUnauthorized} from "@/lib/mobile-auth";
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){const user=await getMobileUser(request);if(!user)return mobileUnauthorized();const {id}=await params;const destination=await prisma.destination.findFirst({where:{id,userId:user.id}});if(!destination)return Response.json({ok:false,error:"DESTINATION_NOT_FOUND"},{status:404});if(destination.type==="PROFILE"||destination.type==="VCF")return Response.json({ok:false,error:"SYSTEM_DESTINATION_REQUIRED"},{status:409});if(await prisma.card.count({where:{activeDestinationId:id}}))return Response.json({ok:false,error:"DESTINATION_IN_USE"},{status:409});await prisma.destination.delete({where:{id}});return Response.json({ok:true})}
