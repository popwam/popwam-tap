import Link from "next/link";import {prisma} from "@popwam/db";import {PageHeading} from "@/components/page-heading";import {Badge} from "@/components/badge";
export default async function CardBatchesPage(){const batches=await prisma.cardBatch.findMany({include:{supplier:true,_count:{select:{cards:true}}},orderBy:{createdAt:"desc"}});return <><PageHeading eyebrow="Cards" title="دفعات البطاقات / Card batches" description="إنشاء وتتبع دفعات البرمجة والتفعيل." action={<Link className="btn-primary" href="/admin/cards/batches/new">دفعة جديدة / New batch</Link>}/><div className="grid gap-4">{batches.map(batch=><Link className="card flex flex-wrap items-center justify-between gap-4 p-5" href={`/admin/cards/batches/${batch.id}`} key={batch.id}><div><h2 className="font-bold">{batch.name}</h2><p className="mt-1 text-sm text-slate-500">{batch.supplier?.name||"No supplier"} · {batch.createdAt.toLocaleString()}</p></div><div className="flex gap-3"><Badge value={batch.cardType}/><strong>{batch._count.cards} / {batch.quantity}</strong></div></Link>)}</div></>}
