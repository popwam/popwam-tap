import { prisma } from "@popwam/db";
import { CheckCircle2, CircleAlert, MessageSquareText } from "lucide-react";
import { AdminSmsTestForm } from "@/components/admin-sms-test-form";
import { PageHeading } from "@/components/page-heading";
import { getOtpTestModeSummary } from "@/lib/otp-test-mode";
import { getI18n } from "@/lib/i18n";
import { getSmsRuntimeSettings } from "@/lib/sms/runtime";
import { saveSmsRuntimeSettings } from "@/app/sms-actions";

export const metadata={title:"SMS status"};
export const dynamic="force-dynamic";

export default async function AdminSmsPage(){
  const testMode=getOtpTestModeSummary();
  const [{locale,dictionary:d},last,successful,failed,runtime]=await Promise.all([getI18n(),prisma.otpSendLog.findFirst({orderBy:{createdAt:"desc"},select:{createdAt:true,responseCode:true,messageId:true,cost:true}}),prisma.otpSendLog.count({where:{status:"SENT"}}),prisma.otpSendLog.count({where:{status:"FAILED"}}),getSmsRuntimeSettings()]);const provider=(runtime.providerMode==="environment"?process.env.SMS_PROVIDER||"development":runtime.providerMode).toLowerCase();
  const configured=provider==="smsmisr"?Boolean(process.env.SMSMISR_ENVIRONMENT&&process.env.SMSMISR_USERNAME&&process.env.SMSMISR_PASSWORD&&process.env.SMSMISR_SENDER_TOKEN&&process.env.SMSMISR_TEMPLATE_TOKEN&&process.env.SMSMISR_BASE_URL):Boolean(process.env.SMS_API_URL&&process.env.SMS_API_TOKEN&&process.env.SMS_SENDER_ID);
  const copy=d.adminPages.sms;const common=d.adminPages.common;const yesNo=(value:boolean)=>value?common.yes:common.no;const unavailable=common.notAvailable;
  const values=[provider==="smsmisr"?"SMS Misr":provider,process.env.SMSMISR_ENVIRONMENT==="1"?common.live:process.env.SMSMISR_ENVIRONMENT==="2"?common.test:unavailable,yesNo(configured),yesNo(Boolean(process.env.SMSMISR_SENDER_TOKEN)),yesNo(Boolean(process.env.SMSMISR_TEMPLATE_TOKEN)),last?.createdAt.toLocaleString(locale==="ar"?"ar-EG":"en-US")||unavailable,last?.responseCode||unavailable,last?.messageId||unavailable,last?.cost||unavailable,String(successful),String(failed)];
  const testValues=[yesNo(testMode.enabled),String(testMode.allowlistedPhoneCount),yesNo(testMode.exposedInStaging)];
  return <><PageHeading eyebrow={copy.eyebrow} title={copy.title} description="Secret provider credentials stay in environment variables. Runtime behavior below is stored in the database and takes effect on the next request without a restart."/><form action={saveSmsRuntimeSettings} className="card mb-6 grid gap-4 p-5 sm:grid-cols-2"><label className="flex items-center gap-2"><input type="checkbox" name="enabled" defaultChecked={runtime.enabled}/>SMS enabled</label><label className="flex items-center gap-2"><input type="checkbox" name="testMode" defaultChecked={runtime.testMode}/>Provider test mode label</label><label><span className="label">Sender name</span><input className="input" name="senderName" defaultValue={runtime.senderName}/></label><label><span className="label">Provider mode</span><select className="input" name="providerMode" defaultValue={runtime.providerMode}><option value="environment">From environment</option><option value="development">Development</option><option value="webhook">Webhook</option><option value="smsmisr">SMS Misr</option></select></label><label><span className="label">Default language</span><select className="input" name="defaultLanguage" defaultValue={runtime.defaultLanguage}><option value="ar">العربية</option><option value="en">English</option></select></label><label><span className="label">Country rules</span><input className="input" name="countryRules" defaultValue={runtime.countryRules}/></label><label><span className="label">Arabic OTP template</span><textarea className="input min-h-24" name="templateAr" defaultValue={runtime.templateAr}/></label><label><span className="label">English OTP template</span><textarea className="input min-h-24" name="templateEn" defaultValue={runtime.templateEn}/></label><p className="text-xs text-slate-500 sm:col-span-2">Use {"{code}"} for the OTP and {"{minutes}"} for its lifetime. Passwords, API tokens, sender tokens, and template tokens are never stored here.</p><button className="btn-primary sm:col-span-2">Save runtime settings</button></form>{process.env.SMSMISR_ENVIRONMENT==="2"&&<div className="mb-5 flex gap-3 rounded-2xl border border-amber-400/20 bg-amber-400/10 p-4 text-amber-100"><CircleAlert className="shrink-0"/><p>{copy.testWarning}</p></div>}<section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{copy.labels.map((label,index)=><div className="card p-5" key={label}><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}</p>{configured?<CheckCircle2 size={17} className="text-emerald-300"/>:<MessageSquareText size={17} className="text-slate-500"/>}</div><p className="mt-3 break-all font-semibold" dir={index===7?"ltr":undefined}>{values[index]}</p></div>)}</section><section className="mt-6"><h2 className="mb-3 text-lg font-bold">{copy.testTitle}</h2>{testMode.malformed&&<p className="mb-3 rounded-xl border border-red-400/20 bg-red-500/10 p-3 text-sm text-red-200">{copy.malformed}</p>}<div className="grid gap-3 sm:grid-cols-3">{copy.testLabels.map((label,index)=><div className="card p-5" key={label}><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-3 font-semibold">{testValues[index]}</p></div>)}</div></section><div className="mt-6"><AdminSmsTestForm copy={copy}/></div></>;
}
