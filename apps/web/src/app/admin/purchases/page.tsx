import Link from "next/link";import {prisma} from "@popwam/db";import {PageHeading} from "@/components/page-heading";import {Badge} from "@/components/badge";
export default async function PurchasesPage(){const purchases=await prisma.purchase.findMany({include:{supplier:true,_count:{select:{items:true}}},orderBy:{purchaseDate:"desc"}});const total=purchases.reduce((sum,x)=>sum+Number(x.totalCost),0);return <><PageHeading eyebrow="Purchasing" title="المشتريات / Purchases" description={`Total purchase value: ${total.toFixed(2)}`} action={<Link className="btn-primary" href="/admin/purchases/new">شراء جديد / New purchase</Link>}/><div className="card overflow-x-auto"><table className="w-full text-sm"><thead><tr>{["Date","Supplier","Invoice","Status","Items","Total","Paid","Action"].map(x=><th className="p-3 text-start" key={x}>{x}</th>)}</tr></thead><tbody>{purchases.map(x=><tr className="border-t border-white/10" key={x.id}><td className="p-3">{x.purchaseDate.toLocaleDateString()}</td><td className="p-3">{x.supplier.name}</td><td className="p-3">{x.invoiceNumber||"—"}</td><td className="p-3"><Badge value={x.status}/></td><td className="p-3">{x._count.items}</td><td className="p-3">{String(x.totalCost)}</td><td className="p-3">{String(x.paidAmount)}</td><td className="p-3"><Link className="text-brand-400" href={`/admin/purchases/${x.id}`}>View</Link></td></tr>)}</tbody></table></div></>}
