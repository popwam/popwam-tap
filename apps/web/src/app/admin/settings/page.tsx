import { PageHeading } from "@/components/page-heading";
export default function SettingsPage() { return <><PageHeading eyebrow="System controls" title="System settings" description="Upload policies are enforced server-side and configured with environment variables."/><div className="card max-w-3xl p-5"><dl className="grid gap-4 sm:grid-cols-2">{[["Allowed image types",process.env.ALLOWED_IMAGE_TYPES || "image/jpeg,image/png,image/webp"],["Maximum image size",`${process.env.MAX_IMAGE_UPLOAD_MB || 5} MB`],["Allowed document types",process.env.ALLOWED_FILE_TYPES || "PDF, vCard, common images"],["Maximum file size",`${process.env.MAX_FILE_UPLOAD_MB || 10} MB`],["R2 public base",process.env.R2_PUBLIC_BASE_URL ? "Configured" : "Not configured"],["Public short-link host",process.env.NEXT_PUBLIC_APP_URL || "Not configured"]].map(([key,value]) => <div className="rounded-xl bg-white/5 p-4" key={key}><dt className="text-xs text-slate-500">{key}</dt><dd className="mt-1 break-words text-sm font-semibold">{value}</dd></div>)}</dl></div></>; }
