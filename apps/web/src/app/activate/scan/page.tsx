import { ActivationScanner } from "@/components/activation-scanner";
import { getI18n } from "@/lib/i18n";
export default async function ActivationScanPage({ searchParams }: { searchParams: Promise<{ card?: string }> }) { const [{ locale }, { card = "" }] = await Promise.all([getI18n(), searchParams]); const ar = locale === "ar"; return <main className="landing-shell flex min-h-screen items-center justify-center px-5 py-10"><div className="w-full max-w-xl"><div className="text-center"><p className="text-xs font-black text-brand-400">POPWAM Tap</p><h1 className="mt-3 text-3xl font-black">{ar?"امسح QR التفعيل":"Scan activation QR"}</h1><p className="mt-3 text-sm leading-7 text-slate-400">{ar?"امسح QR الموجود داخل العبوة أو المرفق مع الكارت.":"Scan the QR inside the package or supplied with the card."}</p></div><ActivationScanner publicSlug={card} locale={locale}/></div></main>; }
