import { prisma,ProfileTheme } from "@popwam/db";
import { requireUser } from "@/lib/session";
import { getUserEntitlements } from "@/lib/plans";
import { getI18n } from "@/lib/i18n";
import { updateProfileTheme } from "@/app/actions";
import { ThemePicker } from "@/components/theme-picker";
import { PageHeading } from "@/components/page-heading";
export default async function AppearancePage(){const user=await requireUser();const [{dictionary:d},profile,{effective}]=await Promise.all([getI18n(),prisma.profile.findFirst({where:{userId:user.id,organizationId:null}}),getUserEntitlements(user.id)]);if(!profile)return null;const labels:Record<ProfileTheme,string>={CLASSIC_DARK:d.profile.classicDark,CLASSIC_LIGHT:d.profile.classicLight,ELEGANT_DARK:d.profile.elegantDark,ELEGANT_LIGHT:d.profile.elegantLight,BUSINESS_DARK:d.profile.businessDark,BUSINESS_LIGHT:d.profile.businessLight};return <><PageHeading eyebrow="Appearance" title={d.profile.theme} description="Preview every visual token before applying it to the public profile."/><form action={updateProfileTheme} className="card max-w-4xl p-5 sm:p-7"><input type="hidden" name="profileId" value={profile.id}/><ThemePicker current={profile.theme} canChange={effective.allowThemes} labels={labels}/><div className="sticky bottom-4 mt-6 flex gap-3 rounded-2xl border border-white/10 bg-ink/90 p-3 backdrop-blur"><button className="btn-primary" disabled={!effective.allowThemes}>{d.common.save}</button><a href={profile.slug?`/p/${profile.slug}`:`/p/id/${profile.id}`} target="_blank" className="btn-secondary">{d.common.preview}</a></div></form></>;}
