import {prisma} from "@popwam/db";import {getI18n} from "@/lib/i18n";import {requireUser} from "@/lib/session";import {PageHeading} from "@/components/page-heading";import {PwaClient} from "@/components/pwa-client";import {GoogleLinkButton} from "@/components/google-link-button";
export default async function SettingsPage(){const user=await requireUser();const [{dictionary:d,locale},google]=await Promise.all([getI18n(),prisma.account.findFirst({where:{userId:user.id,provider:"google"},select:{id:true}})]);const ar=locale==="ar";const googleEnabled=Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET);return <><PageHeading eyebrow="Settings" title={d.nav.settings} description={ar?"إعدادات الدخول والتطبيق والخصوصية.":"Sign-in, app and privacy settings."}/><PwaClient mode="settings" installLabel={d.pwa.install}/>{googleEnabled&&<div className="card mt-5 p-5"><h2 className="font-bold">Google</h2><p className="mt-2 text-sm leading-6 text-slate-400">{google?(ar?"تم ربط Google بهذا الحساب.":"Google is linked to this account."):(ar?"الربط اختياري ويتطلب أن تكون مسجلاً في حسابك وأن تؤكد Google بنفسك. لا يتم الدمج بالبريد وحده.":"Linking is optional and requires an authenticated session plus your explicit Google confirmation. Email alone never merges accounts.")}</p>{!google&&<div className="mt-4"><GoogleLinkButton locale={locale}/></div>}</div>}<div className="card mt-5 p-5"><h2 className="font-bold">{ar?"الخصوصية والعمل دون اتصال":"Privacy and offline behavior"}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{ar?"لا يحفظ عامل الخدمة استجابات لوحة التحكم أو الإدارة أو API الخاصة. وتظل المصادقة خاضعة لسياسة جلسة المتصفح.":"Private dashboard, admin and API responses are never stored by the service worker. Authentication remains controlled by the browser session policy."}</p></div></>}
