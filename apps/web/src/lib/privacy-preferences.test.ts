import {describe,expect,it} from "vitest";import {activityIdentityLabel,nearbyEnabled} from "./privacy-preferences";
describe("privacy preferences",()=>{it("keeps activity identity anonymous without explicit consent",()=>{expect(activityIdentityLabel(false,"Mamdouh")).toBe("Anonymous visitor");});it("requires opt-in and a live visibility window for nearby",()=>{const now=new Date("2026-07-18T12:00:00Z");expect(nearbyEnabled(true,new Date("2026-07-18T13:00:00Z"),now)).toBe(true);expect(nearbyEnabled(false,new Date("2026-07-18T13:00:00Z"),now)).toBe(false);expect(nearbyEnabled(true,new Date("2026-07-18T11:00:00Z"),now)).toBe(false);});});
