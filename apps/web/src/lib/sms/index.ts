import "server-only";
import {maskPhone} from "@/lib/phone";
export type SmsOtpInput={to:string;code:string;expiresMinutes:number;locale:"ar"|"en"};
export type SmsDelivery={status:"SENT"|"FAILED";provider:string;messageId?:string};
export interface SmsProvider{name:string;sendOtp(input:SmsOtpInput):Promise<SmsDelivery>}
class DevelopmentSmsProvider implements SmsProvider{name="development";async sendOtp(input:SmsOtpInput):Promise<SmsDelivery>{if(process.env.NODE_ENV==="production")throw new Error("DEVELOPMENT_SMS_DISABLED");console.info("Development OTP delivery",{to:maskPhone(input.to),expiresMinutes:input.expiresMinutes,status:"SENT"});return{status:"SENT",provider:this.name,messageId:`dev-${Date.now()}`}}}
class WebhookSmsProvider implements SmsProvider{name="webhook";async sendOtp(input:SmsOtpInput):Promise<SmsDelivery>{const url=process.env.SMS_API_URL;const token=process.env.SMS_API_TOKEN;if(!url||!token)throw new Error("SMS_PROVIDER_NOT_CONFIGURED");const message=input.locale==="ar"?`رمز POPWAM Tap هو ${input.code}. صالح لمدة ${input.expiresMinutes} دقائق.`:`Your POPWAM Tap code is ${input.code}. It expires in ${input.expiresMinutes} minutes.`;const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({to:input.to,message,senderId:process.env.SMS_SENDER_ID||"POPWAM"}),cache:"no-store"});if(!response.ok)return{status:"FAILED",provider:this.name};const result=await response.json().catch(()=>({})) as {messageId?:unknown};return{status:"SENT",provider:this.name,messageId:typeof result.messageId==="string"?result.messageId:undefined}}}
export function getSmsProvider():SmsProvider{const provider=(process.env.SMS_PROVIDER||"development").toLowerCase();if(provider==="development"||provider==="test")return new DevelopmentSmsProvider();if(provider==="webhook")return new WebhookSmsProvider();throw new Error("SMS_PROVIDER_UNKNOWN")}
